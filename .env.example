# Copy this file to .env for local development:
#   cp .env.example .env

# Required — shared secret the launcher sends as X-API-Key header.
API_MASTER_KEY=change-me-local-dev-secret

# Optional — defaults shown below.
# HOST=127.0.0.1
# PORT=8080
# RUST_LOG=info
